import Foundation

enum AppConstants {
    static let defaultRefreshInterval: TimeInterval = 30
    static let defaultRemoteCheckInterval: TimeInterval = 300
    static let defaultMaxDiscoveryDepth = 3
    static let appName = "Uncommit"
}
